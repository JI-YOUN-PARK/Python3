i=input()

r=int(i[1])
c=int(ord(i[0]))-int(ord('a')) +1

st=[(2,-1),(1,2),(2,1),(-1,2),(-2,1),(-2,-1),(-1,-2),(1,-2)]

total=0
for k in st:
    nrow=row+k[0]
    ncol=coloum+k[1]

    if ncol>=1 and ncol<=8 and nrow >=1 and nrow<=8 :
        total+=1
print(total)
